@import '../variables.styl';

.jac-theme-yellow {
	a,
	p a code,
	.theme-default-content code {
		color: $yellowAccentColor;
	}

	// @parent-theme/components/AlgoliaSearchBox.vue
	.algolia-search-wrapper .algolia-autocomplete {
		.algolia-docsearch-suggestion {
			.algolia-docsearch-suggestion--category-header {
				background-color: darken($yellowAccentColor, 10%);
			}

			.algolia-docsearch-suggestion--subcategory-column {
				.algolia-docsearch-suggestion--highlight {
					color: $yellowAccentColor;
				}
			}
		}

		.algolia-docsearch-suggestion--text .algolia-docsearch-suggestion--highlight {
			box-shadow: inset 0 -2px 0 0 rgba($yellowAccentColor, 0.8);
		}

		.algolia-docsearch-suggestion--highlight {
			color: $yellowAccentColor;
		}
	}

	// @parent-theme/components/Home.vue
	.hero .action-button {
		background-color: $yellowAccentColor;
		border-bottom-color: darken($yellowAccentColor, 10%);

		&:hover {
			background-color: lighten($yellowAccentColor, 10%);
		}
	}

	// @parent-theme/components/NavLinks.vue
	.nav-links {
		a,
		.dropdown-wrapper .dropdown-title,
		.dropdown-wrapper .mobile-dropdown-title,
		.dropdown-wrapper .nav-dropdown .dropdown-item a {
			color: inherit;

			&:hover,
			&.router-link-active {
				color: $yellowAccentColor;
			}
		}

		.dropdown-wrapper .nav-dropdown .dropdown-item a.router-link-active::after {
			border-left-color: $yellowAccentColor;
		}
	}

	@media (min-width: $MQMobile) {
		.nav-item > a:not(.external) {
			&:hover,
			&.router-link-active {
				border-bottom-color: $yellowAccentColor;
			}
		}
	}

	// @parent-theme/components/PageEdit.vue
	.page-edit {
		.edit-link a {
			color: $yellowAccentColor;
		}

		.last-updated .prefix {
			color: $yellowAccentColor;
		}
	}

	// @vuepress/plugin-search/SearchBox.vue
	.search-box {
		input:focus {
			border-color: alpha($yellowAccentColor, 0.75);
		}

		.suggestion {
			&.focused a {
				color: $yellowAccentColor;
			}
		}
	}

	// @parent-theme/components/SidebarLink.vue
	&.jac-theme-dark {
		a.sidebar-link {
			color: $darkAltTextColor;
		}
	}

	// @parent-theme/components/SidebarLink.vue
	a.sidebar-link {
		color: $textColor;

		&:hover {
			color: $yellowAccentColor;
		}

		&.active {
			color: $yellowAccentColor;
			border-left-color: $yellowAccentColor;
		}
	}

	// @vuepress/plugin-pwa/lib/SWUpdatePopup.vue
	.sw-update-popup {
		border-color: $yellowAccentColor;
	}

	// @vuepress/plugin-nprogress/nprogress.styl
	#nprogress {
		.bar {
			background-color: $yellowAccentColor;
		}

		.peg {
			box-shadow: 0 0 10px $yellowAccentColor, 0 0 5px $yellowAccentColor;
		}

		.spinner-icon {
			border-top-color: $yellowAccentColor;
			border-left-color: $yellowAccentColor;
		}
	}

	// @vuepress/plugin-back-to-top/BackToTop.vue
	.go-to-top {
		background: $yellowAccentColor !important;
	}

	.theme-code-group__nav-tab-active[data-v-6f2a4370] {
		border-bottom: $yellowAccentColor 1px solid !important;
	}
}
